import styled from 'styled-components';

const RecipeTabContainer = styled.div`
	display: flex;
	justify-content: space-around;
	margin-top: 16px;
	box-shadow: 0px 0px 6px rgba(0, 0, 0, 0.1);
`;

export default RecipeTabContainer;
